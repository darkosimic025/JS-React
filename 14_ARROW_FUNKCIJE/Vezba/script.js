function suma(a, b) {
    return a + b;
} 
// let zbir = suma(2, 4);
// console.log(zbir);

console.log(suma(2, 4));
// _________________________________________________________________________________________________________________________________________________________________________
let suma2 = function (a, b) {
    return a + b;
}
console.log(suma2(2, 4));

// _________________________________________________________________________________________________________________________________________________________________________

let suma3 = (a, b) => a + b;
console.log(suma3(2, 2));

// _________________________________________________________________________________________________________________________________________________________________________

let paragraf = (velicina, tekst) =>
    document.body.innerHTML += `<p style="font-size:${velicina}px"> ${tekst} </p>`;
paragraf(35, `Hello world`);

// _________________________________________________________________________________________________________________________________________________________________________

let pozdrav = (ime, prezime) =>
    document.body.innerHTML += `<p>Pozdrav ${ime} ${prezime}</p>`;
pozdrav(`Darko`, `Simic`);

// _________________________________________________________________________________________________________________________________________________________________________

let ispis = (ime, godine) => {
    if (godine < 18) {
        let poruka = `Korisinik ${ime} je maloletna osoba`
        document.body.innerHTML += `<p style="color:red;">${poruka}</p>`;
    } else {
        let poruka = `Korsinik ${ime} je punoletna osoba`
        document.body.innerHTML += `<p style="color:green;">${poruka}</p>`;
    }
}
ispis(`Darko`, 24);
ispis(`Mika`, 16);

// _________________________________________________________________________________________________________________________________________________________________________

